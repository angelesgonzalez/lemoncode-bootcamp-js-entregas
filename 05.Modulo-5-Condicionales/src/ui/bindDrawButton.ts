// Busca un elemento del DOM
// Le añade un event listener
//  Recibe una función(handler) como parámetro
// Llama a ese handler cuando ocurre el evento



// export const updateHTML = (element: HTMLElement, message: string) => {
// 	element.innerText = `${message}`;
// };
