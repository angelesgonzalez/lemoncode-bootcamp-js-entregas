export const deckCounterUI = document.getElementById(
	"deck",
) as HTMLElement | null;
export const scoreCounterUI = document.getElementById(
	"score",
) as HTMLElement | null;

export const roundCounterUI = document.getElementById(
	"roundTotalCounter",
) as HTMLElement | null;
export const cardsContainer = document.getElementById(
	"cards-container",
) as HTMLElement | null;
export const drawCardBTN = document.getElementById(
	"anothercard",
) as HTMLElement | null;
export const endRoundBTN = document.getElementById(
	"endRound",
) as HTMLElement | null;
